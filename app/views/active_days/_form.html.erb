<%= form_with(model: active_day) do |form| %>
  <% if active_day.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(active_day.errors.count, "error") %> prohibited this active_day from being saved:</h2>

      <ul>
        <% active_day.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :day, style: "display: block" %>
    <%= form.text_field :day %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
