<% content_for :title, "Editing daily session" %>

<h1>Editing daily session</h1>

<%= form_with(model: @daily_session, url: active_day_daily_session_path(@daily_session.active_day, @daily_session)) do |form| %>
  <%= render :partial => "form", :locals => { :form => form, :daily_session => @daily_session } %>
<% end %>

